# Copy to .streamlit/secrets.toml (gitignored) or paste into the Streamlit
# Community Cloud "Secrets" box to enable live Claude narration.
# Without it the app uses the deterministic template narrator.

ANTHROPIC_API_KEY = ""
